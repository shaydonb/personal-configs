#!/bin/bash
# This script sets up the configuration files that will be used by ther respective tools.
# TODO: Write the script to emplace/replace the configuration files.
# TODO: Add features to prevent adding configuration files for packages not present/installed.
# TODO: Add a (flag-prompted?) feature to install certain missing packages.
# TODO: Ask the user if they would like to install each package not yet present.
# TODO: Allow user to run this script even with slight changes tho the configs.
    # TODO: Enable the above feature to only add lines in the git-maintained
    #       config that are missing in the local config, rather than purely
    #       overwriting, in the event that any local changes were made that
    #       aren't desired on all machines.

